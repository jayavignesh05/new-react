<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /v8/
  RewriteCond %{REQUEST_URI} ^/v8/api/ [NC]
  RewriteRule ^ - [L]
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
  RewriteRule ^ index.html [L]
</IfModule>
